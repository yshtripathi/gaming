Keep:
- Actual text content (keep existing translated text without impacting the translation keys)
- Any links or URLs (keep existing routes)
- Form structure (keep existing field names and logic)
- Dont break any backend fucntionality
- dont change any keys
- No Rating, reviews
- No Social media links


IMPORTANT — Libraries and dependencies:
- Do NOT install any new npm packages
- Do NOT install any new composer packages  
- Do NOT add any new CDN links
- Do NOT add new script tags or link tags to the layout
- Only use CSS and JavaScript libraries already loaded 
  in the existing layout file
- Check what is already available before writing any code
- If you need a component (slider, modal, dropdown, tabs) 
  — build it using the existing libraries only
- If something cannot be done with existing libraries, 
  tell me what is missing and ask before adding anything new

For styling:
- Check which CSS framework is already loaded 
  (Bootstrap, Tailwind, custom etc)
- Use existing utility classes from that framework first
- Only write custom CSS for things the existing framework 
  cannot handle
- Add custom CSS in existing CSS files, do not create new ones
- Do not override existing CSS by duplicating rules, 
  extend only what needs changing



For JavaScript:
- Check which JS libraries are already loaded 
  (jQuery, Alpine.js, vanilla JS etc)
- Write new JS in the same style as existing JS in the project
- Do not add new JS files, add to existing ones
- Do not use ES6 imports/modules unless the project 
  already uses them

  Summary msg after all every completed task